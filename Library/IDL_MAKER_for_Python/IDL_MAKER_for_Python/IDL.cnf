// 패킷 번호는 0부터 시작한다.
#PACKETNUM 0


bool SerializeBuffer_netPacketProc_SC_CREATE_MY_CHARACTER (char* ID, #DEST DEST X, int Y)

bool SerializeBuffer_netPacketProc_CS_ATTACK1(st_SESSION* p_Session, SerializeBuffer* clpPacket)



// 여기서 생성되는 파일 proxy.h proxy.cpp stub.h stub.cpp
// proxy.h
// 


// 직렬화 버퍼에서 여기에 작성할 때, destID가 여기에 작성되면 안된다. 
// 하지만 proxy.cpp에는 destID를 작성할 수 있는 매개변수가 있어야 한다. 

// 또한 Stub에도 여기에는 없는 fromID가 따로 생겨야 한다. 